#include <stdio.h>
int main ()
{
    int i;

   //while (i<=10)
   {
       printf ("%d\n",i);
       i++
   }
   //for (i=1;i<=10;i++)
   {
       printf ("%d\n",i);
   }
   return 0;
}

        //*Initiziling is not mandatory if we have already
        //*But semicolumb is
        //*Section is imp. in (for)
